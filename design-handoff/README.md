# Claude Design handoff (original export)

This folder is the untouched export from Claude Design that this repository implements.
`project/Campus Management System.dc.html` is the source of truth for the visual design;
`chats/` records how the user and the design assistant arrived at it.

Nothing here is built or deployed — see the root README for the implementation.
