# Unified Vercel deployment: leave this unset. The static frontend calls the
# Express API on the same domain through `/api/*`.
#
# Split deployment or GitHub Pages: set the public URL of the separately hosted
# API, with no trailing slash. NEXT_PUBLIC values are baked in at build time.
# NEXT_PUBLIC_API_BASE="https://campus-connect-api.vercel.app"
