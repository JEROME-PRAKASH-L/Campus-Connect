# Base URL of the API, with no trailing slash. Baked in at build time, so a
# change needs a rebuild rather than just a restart.
NEXT_PUBLIC_API_BASE="http://localhost:4000"

# Deployed: point this at the API's Vercel domain, e.g.
#   NEXT_PUBLIC_API_BASE="https://campus-connect-api.vercel.app"
