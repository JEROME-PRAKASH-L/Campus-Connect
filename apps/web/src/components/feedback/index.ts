/**
 * Loading, empty, error, success and permission states come from the shared UI
 * package so every screen in the portal speaks with the same voice.
 */
export { EmptyState, ErrorState, PermissionNotice, Spinner, SuccessBanner } from '@campus-connect/ui';
